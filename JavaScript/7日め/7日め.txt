(1) Dateオブジェクトの使い方

  const now = new Date();    // 現在
  const past = new Date(1985, 6, 1);   // 過去
  const period = now.getTime();   // 1970.1.1 からの経過ミリ秒

(2) Mathオブジェクトの使い方
  Math.PI
  Math.floor()

(3) カウントダウンタイマーの作り方
  setTimeout()メソッド
  setInterval()メソッド

(4) セレクトメニューを変更した時点で function() を実行する。(onchange()メソッド)



修正時刻: Fri 2023/03/10 16:40:39
